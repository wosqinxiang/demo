spring:
  application:
    name: "@artifactId@"
  profiles:
    active: "@spring.profiles.active@"

jasypt:
  encryptor:
    password: AHdms
    property:
      prefix: Digest(

dms:
  server:
    shortName: SVSTEST